[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "uc-intg-emby"
description = "Emby Media Server Integration for Unfolded Circle Remote Two/3"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MPL-2.0"}
authors = [
    {name = "Meir Miyara", email = "meir.miyara@gmail.com"}
]
keywords = [
    "unfoldedcircle",
    "integration",
    "emby",
    "media-player",
    "remote-control"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Home Automation",
    "Topic :: Multimedia :: Video",
]

dependencies = [
    "ucapi>=0.7.0",
    "ucapi-framework>=1.9.4",
    "aiohttp>=3.9.0",
    "certifi>=2023.0.0",
]
dynamic = ["version"]

[project.urls]
"Homepage" = "https://github.com/mase1981/uc-intg-emby"
"Bug Reports" = "https://github.com/mase1981/uc-intg-emby/issues"
"Source" = "https://github.com/mase1981/uc-intg-emby"

[tool.setuptools]
packages = ["uc_intg_emby"]

[tool.setuptools.dynamic]
version = {attr = "uc_intg_emby.__version__"}

[tool.setuptools.package-data]
uc_intg_emby = ["*.json"]
